---
title: 히스토그램 Histogram
description: 히스토그램(histogram)으로 데이터의 개략적인 분포 파악
date: 2026-05-03
date-modified: 2026-05-03
categories: [ggplot2, base graph, histogram]
---

#### 기본 R 패키지 graphics

```{r}
hist(rchisq(100, df = 5))
```

#### 패키지 ggplot2

```{r}
library(ggplot2)
ggplot(data.frame(values = rchisq(100, df = 5)), aes(x = values)) +
  geom_histogram(binwidth = 2)
```
